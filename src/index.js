/**
 * DSH-OS26 host entry.
 *
 * The initial milestone is client-only. Keeping a real host entry lets the
 * profile loader discover package.json#dsh.client without patching DSH core.
 */
export const name = 'dsh-os26'

export function apply() {}

